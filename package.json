{
  "name": "event-creator",
  "version": "1.0.0",
  "description": "Simplify event creations (title, start, and end time). Define the start/end time using local time definition and convert it to ISO time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.3.1"
  },
  "dependencies": {
    "dayjs": "^1.10.7"
  }
}
